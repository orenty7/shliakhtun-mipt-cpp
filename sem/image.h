#include "color.cpp"

const int width = 10;
const int height = 10;

const Color white = Color(1, 1, 1);
const Color red   = Color(1, 0, 0);
const Color green = Color(0, 1, 0);
const Color blue  = Color(0, 0, 1);

const Color background = Color(0.5);
